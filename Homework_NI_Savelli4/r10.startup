/etc/init.d/networking restart
echo 1 > /proc/sys/net/ipv4/ip_forward
#punto 3
ip route add default via 151.0.45.0

# NAT punto 4
# Maschera il traffico in uscita verso r7
iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
